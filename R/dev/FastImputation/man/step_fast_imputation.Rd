% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_fast_imputation.R
\name{step_fast_imputation}
\alias{step_fast_imputation}
\title{Imputation step for use in a \code{recipes} pipeline}
\usage{
step_fast_imputation(
  recipe,
  ...,
  constraints = list(),
  ignore_cols = NULL,
  categorical = NULL,
  role = NA,
  trained = FALSE,
  patterns = NULL,
  skip = FALSE,
  id = recipes::rand_id("fast_imputation")
)
}
\arguments{
\item{recipe}{A \code{recipe} object.}

\item{...}{<[`tidy-select`][tidyselect::language]> Columns to impute.}

\item{constraints}{A named list of bound constraints passed to
\code{\link{train_fast_imputation}}.}

\item{ignore_cols}{<[`tidy-select`][tidyselect::language]> Columns to
ignore during imputation (e.g. ID columns). Resolved to a character
vector at recipe-creation time.}

\item{categorical}{<[`tidy-select`][tidyselect::language]> Categorical
columns. Resolved to a character vector at recipe-creation time.}

\item{role}{Not used; included for \code{recipes} API compatibility.}

\item{trained}{Set automatically by \code{\link[recipes]{prep}}.}

\item{patterns}{Internal. Populated after \code{\link[recipes]{prep}}.}

\item{skip}{Passed to \code{\link[recipes]{add_step}}.}

\item{id}{Unique step identifier.}
}
\value{
An updated \code{recipe}.
}
\description{
\code{step_fast_imputation} creates a specification of a recipe step that
will impute missing values using the conditional multivariate normal
approach of \code{\link{train_fast_imputation}} and
\code{\link{fast_imputation}}.
}
\examples{
\dontrun{
library(recipes)
data(FI_train)
data(FI_test)

rec <- recipe(~., data = FI_train) |>
  step_fast_imputation(
    everything(),
    constraints = list(bounded_below_2 = list(lower = 0)),
    ignore_cols = user_id_1,
    categorical = categorical_9
  )

trained_rec <- prep(rec, training = FI_train)
bake(trained_rec, new_data = FI_test)
}
}
\keyword{internal}
